import React from 'react';
import { createRoot } from 'react-dom/client';
import { TeamGameNoUI } from './TeamGameNoUI';

const root = createRoot(document.getElementById('root')!);
root.render(<TeamGameNoUI />);
