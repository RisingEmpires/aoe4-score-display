import React from 'react';
import { createRoot } from 'react-dom/client';
import { TeamGameIOU } from './TeamGameIOU';

const root = createRoot(document.getElementById('root')!);
root.render(<TeamGameIOU />);
